# Similarity Detection Rules

Human-curated principles the related-feedback detector applies on **every** call — appended to its
system prompt at startup (see `src/core/promptGuidance.ts`). Distilled *by a human* from confirmed
/ rejected links, never auto-appended. Full rationale: `DISTILLED-RULES-PRD.md`.

_(none yet — add a rule here as false-positive or false-negative link patterns emerge in review,
e.g. "near-identical wording isn't required; same underlying issue in different words counts")_
